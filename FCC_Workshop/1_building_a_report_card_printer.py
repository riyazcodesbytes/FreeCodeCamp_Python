# I will practice Python data types by buidling a simple report card printer. 
# FUNCTION, is a reusable block of code that can be called, or invoked, to run its code, adn arguments can be passed to it. 
# print(name)    # print() = function & name = argument

# Student name declaration
name = "Riyaz" 
print(name, type(name)) # prnting student name and data type

is_student = True # is student currently enrolled?
print(is_student, type(is_student))

age = 20 # student age declaration
print(age, type(age))

# student score is declared
score = 80.5
print(isinstance(score, int))
print(isinstance(score, float))
print(score, type(score))